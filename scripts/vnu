#!/usr/bin/env bash
set -euo pipefail

readonly vnu_jar="/opt/vnu/vnu.jar"

if [ ! -r "$vnu_jar" ]; then
  echo "ERROR: pinned Nu HTML Checker artifact is unavailable: ${vnu_jar}" >&2
  exit 1
fi

exec java -jar "$vnu_jar" "$@"
